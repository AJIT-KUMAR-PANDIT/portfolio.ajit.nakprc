name = "ajitkumarpandit"
compatibility_date = "2025-10-15"

type = "javascript"
compatibility_flags = ["nodejs_compat"] 

[build]
command = "npm install && npm run build"
[build.upload]
dir = ".next"       
format = "service-worker"
